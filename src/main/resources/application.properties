# JPA Props
spring.jpa.hibernate.ddl-auto=update
spring.jpa.generate-ddl=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Environment Variable Defaults
#DB_HOST=localhost:3306
#DB_NAME=users
#DB_USER=user
#DB_PWD=password

# Database Props
spring.datasource.url=jdbc:mysql://${DB_HOST}:3306/${DB_NAME}?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PWD}
#spring.datasource.driver-class-name=com.mysql.jdbc.Driver

# SQL Logging
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true
#logging.level.org.hibernate.SQL=DEBUG
#logging.level.org.hibernate.type.descriptor.sql.BasicBinder=trace

# Add this property to automatically pick up ConfigMap.
#spring.application.name=user-accounts-feature

# Don't think the following lines are necessary?
#secret.DB_HOST=${SECRET_HOST}
#secret.DB_NAME=${SECRET_NAME}
#secret.DB_USER=${SECRET_USER}
#secret.DB_PASSWORD=${SECRET_PASSWORD}